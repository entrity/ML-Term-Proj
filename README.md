# Deep-Unsupervised-Siamese-Network
For ECS 271. Can a siamese terminal layer be effectively (usefully) trained in an unsupervised manner? This approach is based on deep embedding for clustering with SAE's.
